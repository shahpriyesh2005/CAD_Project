<%= form_with(model: [@wishlist], local: true) do |form| %>
  <% if wishlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wishlist.errors.count, "error") %> prohibited this wishlist from being saved:</h2>

      <ul>
        <% wishlist.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :wishlist_date %>
    <%= form.date_select :wishlist_date %>
  </div>

  <div class="field">
  <%= form.label :event_id %>
  <%= form.select(:event_name, Wishlist::EVENT_NAME) %>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
