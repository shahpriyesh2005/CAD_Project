<%= form_with(model: [@user,@subscription], local: true) do |form| %>
  <% if subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
        <% subscription.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <%= form.label :subscribed_user_id %>
  <%= form.select(:subscribed_user_id, Subscription::ORGANIZER_NAME) %>
</div>

  <div class="field">
    <%= form.label :subscription_date %>
    <%= form.date_select :subscription_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
