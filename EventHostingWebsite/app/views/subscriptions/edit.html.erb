<h1>Editing Subscription</h1>

<%= render 'form', subscription: @subscription %>

<%= link_to 'Show', user_subscription_path(@user, @subscription) %> |
<%= link_to 'Back', user_subscriptions_path(@user) %>
